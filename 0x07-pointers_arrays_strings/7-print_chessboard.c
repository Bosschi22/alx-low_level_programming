#include "main.h"
/**
 * print_chessboard - Entry point
 * @a: array
 * Return: Always 0
 */

void print_chessboard(char (*a)[8])
{
int c;
int b;
	for (c = 0; c < 0; c++)
	{
		for (b = 0; b < 0; b++)
		_putchar(a[c][b]);
	_putchar('\n');
	}
}
